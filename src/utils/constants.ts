import { ZtStock } from '../types';

export const DEFAULT_ZT_POOL: ZtStock[] = [
  {
    code: '001266', name: '宏英智能', sealTime: '09:30', openCount: 0,
    marketCap: 20.9, sector: '自动化设', boardLevel: 1, catalystLevel: 'B' as const,
    sealAmountRatio: 0.0, dragonTiger: false, timestamp: '20260428',
    changeRate: 10.0,
  },
  {
    code: '000889', name: '中嘉博创', sealTime: '09:30', openCount: 0,
    marketCap: 42.7, sector: '通信服务', boardLevel: 1, catalystLevel: 'B' as const,
    sealAmountRatio: 0.0, dragonTiger: false, timestamp: '20260428',
    changeRate: 10.0,
  },
  {
    code: '600227', name: '赤天化', sealTime: '09:30', openCount: 0,
    marketCap: 51.7, sector: '农化制品', boardLevel: 1, catalystLevel: 'B' as const,
    sealAmountRatio: 0.0, dragonTiger: false, timestamp: '20260428',
    changeRate: 10.0,
  },
  {
    code: '600488', name: '津药药业', sealTime: '09:30', openCount: 0,
    marketCap: 67.5, sector: '化学制药', boardLevel: 1, catalystLevel: 'B' as const,
    sealAmountRatio: 0.0, dragonTiger: false, timestamp: '20260428',
    changeRate: 10.0,
  },
  {
    code: '600152', name: '维科技术', sealTime: '09:30', openCount: 0,
    marketCap: 83.0, sector: '电池', boardLevel: 3, catalystLevel: 'B' as const,
    sealAmountRatio: 0.0, dragonTiger: false, timestamp: '20260428',
    changeRate: 10.0,
  },
  {
    code: '600156', name: '华升股份', sealTime: '09:30', openCount: 0,
    marketCap: 40.8, sector: '纺织制造', boardLevel: 1, catalystLevel: 'B' as const,
    sealAmountRatio: 0.0, dragonTiger: false, timestamp: '20260428',
    changeRate: 10.0,
  },
  {
    code: '600320', name: '振华重工', sealTime: '09:30', openCount: 0,
    marketCap: 179.7, sector: '专用设备', boardLevel: 1, catalystLevel: 'B' as const,
    sealAmountRatio: 0.0, dragonTiger: false, timestamp: '20260428',
    changeRate: 10.0,
  },
  {
    code: '001298', name: '好上好', sealTime: '09:30', openCount: 0,
    marketCap: 55.7, sector: '其他电子', boardLevel: 1, catalystLevel: 'B' as const,
    sealAmountRatio: 0.0, dragonTiger: false, timestamp: '20260428',
    changeRate: 10.0,
  },
  {
    code: '600770', name: '综艺股份', sealTime: '09:30', openCount: 0,
    marketCap: 99.6, sector: '综合Ⅱ', boardLevel: 1, catalystLevel: 'B' as const,
    sealAmountRatio: 0.0, dragonTiger: false, timestamp: '20260428',
    changeRate: 10.0,
  },
  {
    code: '000967', name: '盈峰环境', sealTime: '09:30', openCount: 0,
    marketCap: 456.5, sector: '环保设备', boardLevel: 1, catalystLevel: 'B' as const,
    sealAmountRatio: 0.0, dragonTiger: false, timestamp: '20260428',
    changeRate: 10.0,
  },
  {
    code: '600623', name: '华谊集团', sealTime: '09:30', openCount: 0,
    marketCap: 202.8, sector: '化学原料', boardLevel: 1, catalystLevel: 'B' as const,
    sealAmountRatio: 0.0, dragonTiger: false, timestamp: '20260428',
    changeRate: 10.0,
  },
  {
    code: '000593', name: '德龙汇能', sealTime: '09:30', openCount: 0,
    marketCap: 91.2, sector: '燃气Ⅱ', boardLevel: 2, catalystLevel: 'B' as const,
    sealAmountRatio: 0.0, dragonTiger: false, timestamp: '20260428',
    changeRate: 10.0,
  },
  {
    code: '600379', name: '宝光股份', sealTime: '09:30', openCount: 0,
    marketCap: 45.8, sector: '电网设备', boardLevel: 1, catalystLevel: 'B' as const,
    sealAmountRatio: 0.0, dragonTiger: false, timestamp: '20260428',
    changeRate: 10.0,
  },
  {
    code: '001358', name: '兴欣新材', sealTime: '09:30', openCount: 0,
    marketCap: 15.4, sector: '化学制品', boardLevel: 1, catalystLevel: 'B' as const,
    sealAmountRatio: 0.0, dragonTiger: false, timestamp: '20260428',
    changeRate: 10.0,
  },
  {
    code: '600744', name: '华银电力', sealTime: '09:30', openCount: 0,
    marketCap: 176.1, sector: '电力', boardLevel: 1, catalystLevel: 'B' as const,
    sealAmountRatio: 0.0, dragonTiger: false, timestamp: '20260428',
    changeRate: 10.0,
  },
  {
    code: '600496', name: '精工钢构', sealTime: '09:30', openCount: 0,
    marketCap: 81.4, sector: '专业工程', boardLevel: 1, catalystLevel: 'B' as const,
    sealAmountRatio: 0.0, dragonTiger: false, timestamp: '20260428',
    changeRate: 10.0,
  },
  {
    code: '600248', name: '陕建股份', sealTime: '09:30', openCount: 0,
    marketCap: 137.1, sector: '房屋建设', boardLevel: 1, catalystLevel: 'B' as const,
    sealAmountRatio: 0.0, dragonTiger: false, timestamp: '20260428',
    changeRate: 10.0,
  },
  {
    code: '600726', name: '华电能源', sealTime: '09:30', openCount: 0,
    marketCap: 524.8, sector: '电力', boardLevel: 4, catalystLevel: 'B' as const,
    sealAmountRatio: 0.0, dragonTiger: false, timestamp: '20260428',
    changeRate: 10.0,
  },
];
export function filterZtPool(pool: ZtStock[]): ZtStock[] {
  return pool.filter(s => {
    if (s.name.includes('ST') || s.name.includes('*ST')) return false;
    if (s.code.startsWith('300') || s.code.startsWith('688')) return false;
    return true;
  });
}

export function getSealTimeLevel(sealTime: string): string {
  if (sealTime <= '09:30') return '🔥一字';
  if (sealTime <= '10:00') return '⚡早板';
  if (sealTime <= '11:30') return '📌午前';
  return '🔴午后';
}

export function getMarketCapLevel(cap: number): 'optimal' | 'caution' | 'risky' {
  if (cap <= 100) return 'optimal';
  if (cap <= 300) return 'caution';
  return 'risky';
}

export function getSealTimeColor(sealTime: string): string {
  if (sealTime <= '09:30') return 'text-green-400';
  if (sealTime <= '10:00') return 'text-lime-400';
  if (sealTime <= '11:30') return 'text-yellow-400';
  if (sealTime <= '13:00') return 'text-orange-400';
  return 'text-red-400';
}

export const SENTIMENT_COLORS: Record<string, string> = {
  '强共振': 'text-red-400',
  '可做': 'text-green-400',
  '谨慎': 'text-yellow-400',
  '回避': 'text-slate-500',
};

export async function importFromCSV(file: File): Promise<Record<string, string>[]> {
  const text = await file.text();
  const lines = text.trim().split('\n');
  if (lines.length < 2) return [];
  const headers = lines[0].split(',').map(h => h.trim().replace(/^"|"$/g, ''));
  return lines.slice(1).map(line => {
    const values = line.split(',').map(v => v.trim().replace(/^"|"$/g, ''));
    const row: Record<string, string> = {};
    headers.forEach((h, i) => { row[h] = values[i] || ''; });
    return row;
  });
}

export function exportToCSV(stocks: Record<string, unknown>[], filename?: string): void {
  const headers = ['代码', '名称', '封板时间', '开板次数', '市值(亿)', '题材', '连板', '催化', '封单比', '龙虎榜', '日期'];
  const rows = stocks.map((s: Record<string, unknown>) => [
    String(s['代码'] ?? s['code'] ?? ''),
    String(s['名称'] ?? s['name'] ?? ''),
    String(s['封板时间'] ?? s['sealTime'] ?? ''),
    String(s['开板次数'] ?? s['openCount'] ?? 0),
    String(s['市值'] ?? s['marketCap'] ?? 0),
    String(s['题材'] ?? s['sector'] ?? ''),
    String(s['连板'] ?? s['boardLevel'] ?? 1),
    String(s['催化等级'] ?? s['catalystLevel'] ?? 'B'),
    String(s['封单比'] ?? s['sealAmountRatio'] ?? 0),
    String(s['龙虎榜'] ?? s['dragonTiger'] ?? false),
    String(s['日期'] ?? s['timestamp'] ?? ''),
  ]);
  const csv = [headers, ...rows].map(r => r.join(',')).join('\n');
  const blob = new Blob(['\ufeff' + csv], { type: 'text/csv;charset=utf-8' });
  const url = URL.createObjectURL(blob);
  const a = document.createElement('a');
  a.href = url;
  a.download = filename || `zt_pool_${new Date().toISOString().slice(0, 10)}.csv`;
  a.click();
  URL.revokeObjectURL(url);
}
